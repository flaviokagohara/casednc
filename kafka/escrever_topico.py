# Aqui como nós possuíamos um função padrão de escrita em tópicos, eu acabei não entrando tão a fundo, 
# então não tenho certeza de como implementar, então vou utilizar o código apenas para escrever

def escrever_em_topicos(TOPIC_NAME, created_at,lista_de_dados,updated_at)
    #função para escrever no tópico